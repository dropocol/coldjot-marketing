---
const currentYear = new Date().getFullYear();

const footerLinks = {
  product: [
    { name: "Features", href: "/features" },
    { name: "Pricing", href: "/pricing" },
    { name: "Blog", href: "/blog" },
    { name: "Help Center", href: "/help" },
  ],
  company: [
    { name: "About", href: "/about" },
    { name: "Privacy Policy", href: "/privacy" },
    { name: "Terms of Service", href: "/terms" },
  ],
  features: [
    { name: "Email Sequences", href: "/features#sequences" },
    { name: "Analytics Dashboard", href: "/features#analytics" },
    { name: "Contact Management", href: "/features#contacts" },
    { name: "Email Templates", href: "/features#templates" },
    { name: "Timeline View", href: "/features#timeline" },
  ],
};
---

<footer class="border-t bg-background">
  <div class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:py-16 lg:px-8">
    <div class="grid grid-cols-2 gap-8 md:grid-cols-4">
      <div class="col-span-2 md:col-span-1">
        <span class="text-xl font-bold">ColdJot</span>
        <p class="mt-4 text-sm text-muted-foreground">
          Streamline your email sequences and automate your outreach with
          powerful analytics.
        </p>
      </div>

      <div>
        <h3 class="text-sm font-medium">Product</h3>
        <ul class="mt-4 space-y-3">
          {
            footerLinks.product.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-muted-foreground hover:text-primary transition-colors"
                >
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-medium">Features</h3>
        <ul class="mt-4 space-y-3">
          {
            footerLinks.features.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-muted-foreground hover:text-primary transition-colors"
                >
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-medium">Company</h3>
        <ul class="mt-4 space-y-3">
          {
            footerLinks.company.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-muted-foreground hover:text-primary transition-colors"
                >
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <div class="mt-12 border-t pt-8">
      <p class="text-center text-sm text-muted-foreground">
        © {currentYear} ColdJot. All rights reserved.
      </p>
    </div>
  </div>
</footer>
