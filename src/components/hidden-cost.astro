---
import { Clock, Target, Inbox, BellOff } from "lucide-react";

interface Problem {
  title: string;
  description: string;
  icon: any;
  iconColor: string;
  colSpan: string;
}

const problems: Problem[] = [
  {
    title: "Cold Emails Get Ignored",
    description:
      "Your prospects are drowning in automated spam. Generic cold emails lack the personalization and timing needed to stand out in crowded inboxes.",
    icon: Target,
    iconColor: "text-rose-600",
    colSpan: "col-span-12 md:col-span-5",
  },
  {
    title: "Manual Work Kills Scale",
    description:
      "You're stuck choosing between quality and quantity. Personalizing each follow-up means you can only reach a fraction of your potential prospects.",
    icon: Clock,
    iconColor: "text-amber-600",
    colSpan: "col-span-12 md:col-span-7",
  },
  {
    title: "Opportunities Slip Through the Cracks",
    description:
      "Without proper timing and sequence automation, even interested prospects slip away. Manual tracking means missed connections and lost deals.",
    icon: Inbox,
    iconColor: "text-blue-600",
    colSpan: "col-span-12 md:col-span-7",
  },
  {
    title: "No Response Insights",
    description:
      "You're flying blind without data on what works. Each failed sequence is a missed opportunity to learn and improve your approach.",
    icon: BellOff,
    iconColor: "text-purple-600",
    colSpan: "col-span-12 md:col-span-5",
  },
];
---

<section class="py-20">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center space-y-6 mb-16">
      <div
        class="inline-flex items-center justify-center px-4 py-1.5 mb-4 border border-primary/10 bg-primary/5 rounded-full"
      >
        <span class="text-sm font-medium text-primary/80">The Problem</span>
      </div>
      <h2 class="text-4xl font-semibold font-display">
        Why Most Cold Emails Fail
      </h2>
      <p class="text-xl text-muted-foreground max-w-3xl mx-auto">
        Every day, millions of cold emails go straight to trash. Here's why your
        outreach might be failing:
      </p>
    </div>

    <div class="grid grid-cols-12 gap-3 mt-14">
      {
        problems.map((problem) => (
          <div
            class={`group flex flex-col rounded-2xl border-[1px] border-slate-200 p-1 shadow-sm ${problem.colSpan}`}
          >
            <div class="flex flex-1 flex-col justify-between gap-3 rounded-xl p-6 bg-gray-50 border-[1px] border-slate-100 transition-colors duration-200 group-hover:border-slate-200">
              <div class="aspect-[4/1] md:aspect-[4/1]">
                <problem.icon
                  className={`h-8 w-8 md:h-10 md:w-10 ${problem.iconColor}`}
                />
              </div>
              <div class="flex flex-col gap-2">
                <h4 class="flex items-center text-lg font-semibold text-gray-900">
                  <span>{problem.title}</span>
                </h4>
                <p class="text-balance text-base text-gray-800">
                  {problem.description}
                </p>
              </div>
            </div>
          </div>
        ))
      }
    </div>

    <div class="mt-16 text-center">
      <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
        The solution isn't sending more emails. It's sending <span
          class="font-semibold text-gray-900">smarter sequences</span
        > with <span class="font-semibold text-gray-900">perfect timing</span>.
      </p>
    </div>
  </div>
</section>
